import React from 'react'
import Modal from "@/components/ui/Modal/Modal";

const addMembers = () => {



    return <React.Fragment>
    <Modal onClose={close} open={open} >
        <p>hello from models</p>
    </Modal>
    </React.Fragment>
}

export default addMembers
